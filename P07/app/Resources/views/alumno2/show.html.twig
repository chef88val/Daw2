{% extends 'base.html.twig' %}

{% block body %}
    <h1>Alumno2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumno2.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ alumno2.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('alumno2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('alumno2_edit', { 'id': alumno2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
